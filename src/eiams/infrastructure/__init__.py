"""Infrastructure layer containing adapters and implementations.

The infrastructure layer provides concrete implementations for
ports and contracts defined in the domain and application layers.
It may depend on external frameworks and libraries.
"""
